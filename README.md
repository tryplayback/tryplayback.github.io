# Playback

Landing page for **Playback** — a small desk display that shows the song, artist, runtime and album art of whatever's playing.

Static site, no build step. Open `index.html` or serve the folder:

```bash
tryplayback.xyz
```

